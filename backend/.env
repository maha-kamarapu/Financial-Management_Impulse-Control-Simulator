# .env file

# JWT Secret for signing the tokens (use a strong, unique secret)
JWT_SECRET=your_secret_key_here

# JWT Expiration time (e.g., 1 hour)
JWT_EXPIRATION=1h

# Database Credentials (optional, depending on your setup)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=mahalakshmi
DB_NAME=impulse_control_simulator
